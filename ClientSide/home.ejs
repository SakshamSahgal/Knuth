<!DOCTYPE html>
<html lang="en">

<%- include('./ReusableComponents/BootstrapCDN.ejs') %>

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Home</title>
</head>


<!-- <link rel="stylesheet" href="/Styles/commom.css"> -->
<link rel="stylesheet" href="/Styles/Home.css">

<body>

  <%- include('./ReusableComponents/navbar.ejs',{ activePage : page, email: emailTo }) %>

  <div class="container mt-5">
    <!-- Button Above the Timeline -->
    <div class="mb-3">
      <button type="button" id="postEventButton" class="btn btn-primary btn-block <%= (coordinator == false) ? 'locked-button' : '' %>" >
        <span class="<%= (coordinator == false) ? '' : 'lockIcon' %>">&#x1F512;</span> Post Event
      </button>
    </div>
    
    <!-- Timeline -->
    <div class="timeline">
      <% for(var i=0;i<events.length;i++) { %>
      <!-- Event Item 1 -->
      
      <!-- <div class="event-card">
        <div class="event-card-header">
            <h5 class="event-title">
                <a href="<%= events[i].eventLink %>" target="_blank"><%= events[i].eventName %></a>
            </h5>
            <button class="delete-btn">
                <span>&#10006;</span>
            </button>
        </div>
        <div class="event-card-content">
            <p class="event-description"><%= events[i].eventDescription %></p>
            <% if (events[i].winners.length) { %>
            <div class="winners-section">
                <h6><strong>Winners:</strong></h6>
                <ul class="winners-list">
                    <% for (var j = 0; j < events[i].winners.length; j++) { %>
                      <% for (const key in events[i].winners[j]) { %>
                      <li><strong><%= key %>:</strong> <%= events[i].winners[j][key] %></li>
                      <% } %>
                    <% } %>
                </ul>
            </div>
            <% } %>
        </div>
        <div class="event-card-footer">
            <div class="footer-info">
                <span class="badge badge-primary">Date:</span> <%= events[i].eventDate %>
                <span class="badge badge-secondary">Posted by:</span> <%= events[i].postedBy %>
            </div>
        </div>
    </div> -->
    <div class="card">
      <!-- Header with Color -->
      <div class="card-header bg-dark text-white d-flex justify-content-between align-items-center event-header">
        <h5 class="card-title"><a href="<%= events[i].eventLink %>" class="linkCSS" target="_blank"><%= events[i].eventName %></a></h5>
        <button class="btn ml-auto" onclick="deleteCard(this)"><span> &#10006; </span></button>
      </div>
      
      <!-- Event Description -->
      <div class="card-body">
        <p class="card-text"><%= events[i].eventDescription %></p>
        
        <% if (events[i].winners.length) { %>
          <div class="winners-section mt-4">
            <h6><strong>Winners:</strong></h6>
            <ul class="winners-list">
              <% for (var j = 0; j < events[i].winners.length; j++) { %>
                <% for (const key in events[i].winners[j]) { %>
                  <li><strong><%= key %> :</strong> <%= events[i].winners[j][key] %></li>
                <% } %>
              <% } %>
            </ul>
          </div>
        <% } %>
      </div>


      <div id="carouselExampleIndicators<%=i%>" class="carousel slide imageCarousel" data-ride="carousel">
        <ol class="carousel-indicators">
          <li data-target="#carouselExampleIndicators<%=i%>" data-slide-to="0" class="active"></li>
          <li data-target="#carouselExampleIndicators<%=i%>" data-slide-to="1"></li>
          <li data-target="#carouselExampleIndicators<%=i%>" data-slide-to="2"></li>
        </ol>
        <div class="carousel-inner">
          <div class="carousel-item active">
            <img class="d-block w-100" src="https://source.unsplash.com/770x590/?cloud" alt="First slide">
          </div>
          <div class="carousel-item">
            <img class="d-block w-100" src="https://source.unsplash.com/900x590/?cloud" alt="Second slide">
          </div>
          <div class="carousel-item">
            <img class="d-block w-100" src="https://source.unsplash.com/770x500/?cloud" alt="Third slide">
          </div>
        </div>
        <a class="carousel-control-prev" href="#carouselExampleIndicators<%=i%>" role="button" data-slide="prev">
          <span class="carousel-control-prev-icon" aria-hidden="true"></span>
          <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carouselExampleIndicators<%=i%>" role="button" data-slide="next">
          <span class="carousel-control-next-icon" aria-hidden="true"></span>
          <span class="sr-only">Next</span>
        </a>
      </div>
      
      <!-- Footer -->
      <div class="card-footer text-muted d-flex justify-content-between">
        <small>Event Date: September 21, 2023</small>
        <small>Posted by: John Doe</small>
      </div>
    </div>
    
    <br>
    
      <% } %>
    </div>
  </div>

</body>

</html>
